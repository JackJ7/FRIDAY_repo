"""FRIDAY's version — bumped when a stage or refinement pass ships.

0.1  Phases 0-2 (core: engine, brain, gate, project tools; terminal REPL)
0.2  Stage 1 (app shell, character, knowledge-gap protocol)
0.3  Stage 2 (accountability: commitments, Needs You panel, briefing, DND)
0.4  Refinement pass (permission tiers, native hotkey, windowless, live
     status console, functional workspace tabs)
0.5  Stage 3 (senses: Gmail read+draft, Calendar read + gated create,
     reactive web lookup — data only, cognition stays local)
0.6  Memory-reliability pass (project status field, memory pass with
     read-before-overwrite guard, memory glyph, History tab, settings panel)
0.7  Stage 4 (project-timeline engine: scope -> milestones, deterministic
     slip math + downstream shifts, panel/briefing integration)
0.8  Method transfer (reasoning scaffolds w/ config intensity, deep_think
     routing, playbook system incl. seeding, ARCHITECTURE.md + CLAUDE.md)
0.9  Relational pass (partner voice, name scarcity, local-time baseline,
     history labels, conversational-only email, duplicate-field guard +
     write-through fsync + brain pollution repair)
"""

__version__ = "0.9.0"
